#include <iostream>
#include <string>

#include "libircclient.h"

namespace TWDevNet {
	class IRC {
		IRC() {
		}
		~IRC() {
		}

		irc_session_t * CreateSession(irc_session_t * callbacks) {

		}

	};
}